const menu=document.querySelector('#menu'),nav=document.querySelector('nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.13});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));window.addEventListener('scroll',()=>{const track=document.querySelector('.image-track');if(track&&window.innerWidth>900){const r=track.getBoundingClientRect();track.style.transform=`translateY(${Math.max(-18,Math.min(18,r.top*.015))}px)`}});document.querySelector('#contactForm')?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('#status').textContent='Thanks — the interface is working. Connect a verified production form endpoint before launch.'});